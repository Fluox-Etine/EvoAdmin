
    /**
     * 获取{NOTES}详情
     * @param Request $request
     * @return Response
     * @date {DATE}
     */
    public function detail(Request $request): Response
    {
        $params = $request->post();
        $result = {UPPER_CAMEL_NAME}Logic::handleDetail($params);
        return renderSuccess($result);
    }