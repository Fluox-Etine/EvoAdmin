
    /**
     * 删除{NOTES}
     * @param Request $request
     * @return Response
     * @date {DATE}
     */
    public function delete(Request $request): Response
    {
        $params = $request->post();
        $result = {UPPER_CAMEL_NAME}Logic::handleDelete($params);
        $result ? return renderSuccess('删除成功') : return renderError('删除失败');
    }